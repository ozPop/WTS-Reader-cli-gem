require 'spec_helper'

describe WTSReader do
  it 'has a version number' do
    expect(WTSReader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
